# Managed by Ansible. Root only. Never commit rendered values.
RESTIC_REPOSITORY={{ backup_restic_repository }}
RESTIC_PASSWORD_FILE={{ backup_restic_password_file }}
RESTIC_CACHE_DIR={{ backup_restic_cache_dir }}
SURVIVABILITY_HC_BACKUP_URL={{ backup_restic_healthchecks_backup_url }}
SURVIVABILITY_HC_MAINTENANCE_URL={{ backup_restic_healthchecks_maintenance_url }}
