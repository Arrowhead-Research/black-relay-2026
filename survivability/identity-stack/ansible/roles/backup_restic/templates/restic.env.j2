# Managed by Ansible. Root only. Never commit rendered values.
export RESTIC_REPOSITORY={{ backup_restic_repository }}
export RESTIC_PASSWORD_FILE={{ backup_restic_password_file }}
export RESTIC_CACHE_DIR={{ backup_restic_cache_dir }}
export SURVIVABILITY_HC_BACKUP_URL={{ backup_restic_healthchecks_backup_url }}
export SURVIVABILITY_HC_MAINTENANCE_URL={{ backup_restic_healthchecks_maintenance_url }}
