---
- name: Restart systemd-journald
  ansible.builtin.systemd_service:
    name: systemd-journald
    state: restarted

- name: Apply host sysctl settings
  ansible.builtin.command: /usr/sbin/sysctl --system
  changed_when: true
