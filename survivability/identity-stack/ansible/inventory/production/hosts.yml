---
# Intentionally contains no production host, address, or SSH details. It is
# used only for credential-free Pi/CI checks. A trusted operator copies
# hosts.example.yml outside the repository, replaces its placeholders, sets
# ANSIBLE_INVENTORY to that protected file, and validates it before deployment.
all:
  children:
    identity_stack:
      hosts: {}
