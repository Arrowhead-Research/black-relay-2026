---
# Intentionally contains no production host, address, or SSH details. An
# operator supplies authoritative inventory from a trusted workstation only.
all:
  children:
    identity_stack:
      hosts: {}
