---
all:
  children:
    vyos_routers:
      hosts:
        vyos:
          ansible_host: "{{ vyos_router_management_host }}"
      vars:
        ansible_connection: ansible.netcommon.network_cli
        ansible_network_os: vyos.vyos.vyos
        ansible_user: vyos
